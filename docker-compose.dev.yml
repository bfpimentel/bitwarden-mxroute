services:
  bitwarden-mxroute-server:
    container_name: bitwarden-mxroute-server
    build:
      context: .
      dockerfile: server/Dockerfile.dev
    ports:
      - "6123:6123"
    env_file: .env.dev
  bitwarden-mxroute-web:
    container_name: bitwarden-mxroute-web
    build:
      context: .
      dockerfile: web/Dockerfile.dev
    ports:
      - "6124:6124"
    env_file: .env.dev
    depends_on:
      - bitwarden-mxroute-server
